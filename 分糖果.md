##
## 解：
* 1、去除重复项
```
var distributeCandies = function(candies) {
  const len = candies.length / 2
  const candy = [...new Set(candies)]
  if(candy.length <= len) {
    return candy.length
  } else {
    return len
  }
};
```